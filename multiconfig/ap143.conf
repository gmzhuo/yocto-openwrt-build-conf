MACHINE = "ap143"
INITRAMFS_IMAGE_BUNDLE = "0"

TCLIBC = "musl"
KERNEL_IMAGETYPES = "uImage"

IMAGE_INSTALL:append = " zygoted servd webd web capwap-WTP"
IMAGE_INSTALL:remove = " quectel-cm f2fs-tools freeradius usb-modeswitch"

TCLIBC = "glibc"
